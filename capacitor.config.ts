
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.webnexus.appshell',
  appName: 'WebNexus',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
